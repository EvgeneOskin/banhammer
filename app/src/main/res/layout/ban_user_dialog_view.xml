<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <Spinner
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/reason_view"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/comment_edit_view"
        android:hint="@string/comment_edit_view_hint"
        android:layout_below="@+id/reason_view" />

    <CheckBox
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/comment_visibility_hint"
        android:id="@+id/comment_visibility"
        android:layout_below="@+id/comment_edit_view" />
</RelativeLayout>